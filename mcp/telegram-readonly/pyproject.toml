[project]
name = "telegram-readonly-mcp"
version = "0.1.0"
description = "Read-only Telegram MTProto MCP server"
requires-python = ">=3.11"
dependencies = [
  "mcp>=1.13.0",
  "python-dotenv>=1.2.2",
  "telethon>=1.41.0",
]

[tool.uv]
package = false
