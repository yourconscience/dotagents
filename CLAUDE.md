# CLAUDE.md

This repo's canonical agent instructions live in `AGENTS.md`.

All agents working in this repository should load and follow `./AGENTS.md` first.

If an agent supports only `CLAUDE.md`-style project memory, treat this file as a compatibility shim and apply the full contents of `AGENTS.md` as the real project instructions.
