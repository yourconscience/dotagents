# Canonical copy managed from a pinned external repository. Review upstream;
# dotagents doctor verifies that this directory still matches its lock pin.
ignore:
  - .git
  - venv
  - .venv
  - env
  - .env
  - .tox
  - node_modules
  - vendor
  - skills/grilling/
